# Makes EntityRenderer.thirdPersonDistance writable so the freelook camera can
# apply its own wall collision (the vanilla raycast follows the real player aim,
# which is the wrong direction while the camera is free-looked).
public net.minecraft.client.renderer.EntityRenderer field_78490_B # thirdPersonDistance